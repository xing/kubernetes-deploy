---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: web
spec:
  replicas: 1
  template:
    metadata:
      labels:
        name: web
        app: test-partials
    spec: <%= partial 'sleepy-pod-spec' %>
<%= partial "pod", name: "pod1", args: ["echo", "foo"] %>
<%= partial "pod", name: "pod2", args: ["echo", "bar"] %>
