---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: web
spec:
  replicas: 1
  template:
    metadata:
      labels:
        name: web
        app: test-partials
    spec:
      containers:
      - name: sleepy-guy
        image: busybox
        imagePullPolicy: IfNotPresent
        command: ["sleep", "8000"]
<%= partial "job", name: "job1", backoff_limit: backoff_limit, args: ["echo", "foo"] %>
<%= partial "job", name: "job2", backoff_limit: backoff_limit, args: ["echo", "bar"] %>
