---
apiVersion: batch/v1
kind: Job
metadata:
  name: <%= name %>
spec:
  template:
    spec:
      containers:
      - name: <%= name %>
        image: busybox
        command: <%= args %>
      restartPolicy: Never
  backoffLimit: <%= locals[:backoff_limit] %>
